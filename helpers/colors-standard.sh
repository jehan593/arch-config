# ==============================================================================
# STANDARD ANSI COLORS — used by setup.sh/reset.sh, which run before the Nord
# theme is applied, so they use the terminal's own color slots instead of
# fixed Nord truecolor values.
# ==============================================================================
export CYAN=$'\e[36m'
export BLUE=$'\e[34m'
export GREEN=$'\e[32m'
export YELLOW=$'\e[33m'
export RED=$'\e[31m'
