// Appended verbatim to the end of user.js by `upf` (after removals are
// applied), so these always win over Betterfox's defaults.
// One user_pref() per line, e.g.:
// user_pref("browser.startup.homepage", "about:blank");
