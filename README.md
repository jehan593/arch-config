# arch-config

Personal Arch Linux dotfiles.

## Setup

Clone anywhere you like, then run setup.sh from within the checkout:

```bash
git clone <your-repo-url> ~/arch-config
bash ~/arch-config/setup.sh
```

## Reset

```bash
bash ~/arch-config/reset.sh
```
